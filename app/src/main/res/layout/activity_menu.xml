<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="com.example.io.myapplication.MenuActivity">

    <TextView
        android:text="@string/menu_title"
        android:id="@+id/menu_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Mapa"
        android:id="@+id/mapa_button"
        android:layout_marginTop="57dp"
        android:layout_below="@+id/contrasenya_text"
        android:layout_centerHorizontal="true" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Editar flota"
        android:id="@+id/flota_button"
        android:layout_below="@+id/mapa_button"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="94dp" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Sortir"
        android:id="@+id/logout_button"
        android:layout_marginTop="88dp"
        android:layout_below="@+id/flota_button"
        android:layout_alignLeft="@+id/mapa_button"
        android:layout_alignStart="@+id/mapa_button" />

</RelativeLayout>
